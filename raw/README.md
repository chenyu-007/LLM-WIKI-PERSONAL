# raw

`raw/` 保存原始资料或来源记录，是 Wiki 的事实依据。

## 使用规则

- 原始文件放在这里，不直接改写成观点。
- 网页、论文、视频等外部资料可以用 `.source.md` 记录来源信息。
- 从 `raw/` 生成的总结、概念解释、关联分析，应写入 `wiki/`。
- 如果原始资料很大，优先保留文件或链接，再在 `wiki/` 中写可读摘要。

## 推荐命名

```text
raw/sources/YYYY-MM-DD-short-title.source.md
raw/files/YYYY-MM-DD-short-title.ext
```

